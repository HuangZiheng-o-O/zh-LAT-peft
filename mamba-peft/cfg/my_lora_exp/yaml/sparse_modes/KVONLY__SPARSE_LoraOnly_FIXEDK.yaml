peft: cfg/my_lora_exp/peft/lora_kv_r8_alpha16.json

env_defaults:
  HP_INIT: pissa
  HP_SAVE_MODE: best_last
  HP_SAVE_FULL_MODEL: 0

# 8-mode batch suite (E30): Sparse Selective Tuning defaults (ENV still wins).
sparse_selective:
  enable: true
  scope: lora_only
  budget_mode: fixed_count
  k: 7569408
  score_samples: 1024

