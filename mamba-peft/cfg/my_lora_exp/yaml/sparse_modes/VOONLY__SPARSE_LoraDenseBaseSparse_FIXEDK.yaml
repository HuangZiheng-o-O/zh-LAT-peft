peft: cfg/my_lora_exp/peft/lora_vo_r8_alpha16.json

env_defaults:
  HP_INIT: pissa
  HP_SAVE_MODE: best_last
  HP_SAVE_FULL_MODEL: 0

sparse_selective:
  enable: true
  scope: lora_dense_base_sparse
  budget_mode: fixed_count
  # K applies only to base sparse deltas; total trainables = dense_LoRA + K
  k: 7569408
  score_samples: 1024

